#include<bits/stdc++.h>
using namespace std;

// Vectors for tracing neighboring elements 8-directionally in a matrix
vector<int> dx = {0, 0, 1, -1, 1, -1, 1, -1};
vector<int> dy = {1, -1, 0, 0, 1, 1, -1, -1};

int main(){

    return 0;
}